I've generated an ORBITAL Intent Document for your React template project. The document defines a single, atomic intent (INT-001) focused on establishing a functional React project foundation.

Key highlights:
- **Outcome:** A working React template that can be cloned and run immediately
- **Trust Tier 1:** Low-risk, reversible, human-informed after execution
- **Acceptance:** 7 testable criteria including successful install, dev server, and build
- **Constraints:** React-based, Node.js compatible, no business logic

The intent deliberately avoids prescribing *how* to implement (e.g., whether to use Create React App, Vite, or another tool) — it focuses purely on the desired outcome: a functional template that developers can build upon.